import React from 'react';
import ReactDOM from 'react-dom/client';
import SalesTracker from './app';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SalesTracker />
  </React.StrictMode>,
);
